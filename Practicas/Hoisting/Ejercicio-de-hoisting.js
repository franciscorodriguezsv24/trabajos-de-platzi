function hello(){
    console.log("Hello" + miNombre);
}

var miNombre = "Fermando"

//Hoisting es una manera de referirse cuando se declara despues de haberse inicializado una variable o function. lo mas recomendable siempre es declarar variables y functions antes de inicializarse para no tener ni un solo error en el  codigo. 